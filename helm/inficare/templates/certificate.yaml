{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .host | replace "." "-" }}-cert
  namespace: {{ $.Release.Namespace }}
spec:
  secretName: {{ if contains "local" .host }}{{ .host | replace "." "-" }}-cert-tls{{ else }}{{ .host }}-cert-tls{{ end }}
  dnsNames:
    - {{ .host }}
  issuerRef:
    name: {{ if contains "local" .host }}selfsigned-issuer{{ else if eq $.Values.app.env "prod" }}letsencrypt-production{{ else }}letsencrypt-staging{{ end }}
    kind: ClusterIssuer
{{- end }}
{{- end }}
